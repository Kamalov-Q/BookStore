import { FC } from "react"

const Spinner: FC = () => {
    return (
        <div className="animate-ping w-16 h-16 m-8 absolute top-[40%] left-1/2  rounded-full bg-sky-600">
        </div>
    )
}

export default Spinner
